<Window x:Class="YTools.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:YTools.UI"
        Title="YTools 设置"
        Width="760"
        Height="560"
        MinWidth="680"
        MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <ui:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
        <Style x:Key="RowLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,6" />
        </Style>
        <Style x:Key="HintText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Background="{DynamicResource InputBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <TabControl x:Name="NavTabs"
                        Background="Transparent"
                        BorderThickness="0"
                        TabStripPlacement="Left">
                <TabItem Header="通用">
                    <Grid />
                </TabItem>
                <TabItem Header="搜索">
                    <Grid />
                </TabItem>
                <TabItem Header="应用别名">
                    <Grid />
                </TabItem>
                <TabItem Header="外观">
                    <Grid />
                </TabItem>
                <TabItem Header="快捷键">
                    <Grid />
                </TabItem>
                <TabItem Header="剪贴板">
                    <Grid />
                </TabItem>
                <TabItem Header="片段">
                    <Grid />
                </TabItem>
                <TabItem Header="系统命令">
                    <Grid />
                </TabItem>
                <TabItem Header="隐私">
                    <Grid />
                </TabItem>
            </TabControl>
        </Border>
        <ContentControl x:Name="PageHost" Grid.Column="1" Margin="22,18" />
    </Grid>
</Window>
